import { CapacitorConfig } from '@capacitor/cli';

// TODO: Replace this bundle ID with your real reverse-domain package name
// before uploading to the Google Play Console (e.g. com.yourcompany.fpo).
const config: CapacitorConfig = {
  appId: 'com.netgecko.fpoapp',
  appName: 'Netgecko',
  webDir: 'dist',
};

export default config;
