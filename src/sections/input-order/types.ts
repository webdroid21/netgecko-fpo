export type InputOrder = {
  id: string;
  createdTime?: string;
  fields: Record<string, any>;
};

export type InputProduct = {
  id: string;
  fields: Record<string, any>;
};

export type Season = {
  id: string;
  fields: Record<string, any>;
};
